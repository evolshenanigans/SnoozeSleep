import { colors } from "./colors";

export const text = {
  heroText: {
    textAlign: "center",
    color: colors.themeWhite,
    fontSize: 18,
  },
  subtitle: {
    textAlign: "center",
    fontWeight: "200",
    fontSize: 14,
    color: colors.themeWhite,
  },
} as const;
